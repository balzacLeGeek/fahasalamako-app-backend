{% extends 'base.html.twig' %}

{% block title %}MedicamentCategory{% endblock %}

{% block body %}
    <h1>MedicamentCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ medicament_category.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ medicament_category.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('medicament_category_index') }}">back to list</a>

    <a href="{{ path('medicament_category_edit', {'id': medicament_category.id}) }}">edit</a>

    {{ include('medicament_category/_delete_form.html.twig') }}
{% endblock %}
