{% extends 'base.html.twig' %}

{% block title %}Rendezvous{% endblock %}

{% block body %}
    <h1>Rendezvous</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rendezvous.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ rendezvous.date ? rendezvous.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Disponible</th>
                <td>{{ rendezvous.disponible ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rendezvous_index') }}">back to list</a>

    <a href="{{ path('rendezvous_edit', {'id': rendezvous.id}) }}">edit</a>

    {{ include('rendezvous/_delete_form.html.twig') }}
{% endblock %}
