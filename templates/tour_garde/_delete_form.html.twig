<form method="post" action="{{ path('tour_garde_delete', {'id': tour_garde.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tour_garde.id) }}">
    <button class="btn">Delete</button>
</form>
