{% extends 'base.html.twig' %}

{% block title %}PrincipesActifs{% endblock %}

{% block body %}
    <h1>PrincipesActifs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ principes_actif.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ principes_actif.nom }}</td>
            </tr>
            <tr>
                <th>Expicients</th>
                <td>{{ principes_actif.expicients }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('principes_actifs_index') }}">back to list</a>

    <a href="{{ path('principes_actifs_edit', {'id': principes_actif.id}) }}">edit</a>

    {{ include('principes_actifs/_delete_form.html.twig') }}
{% endblock %}
