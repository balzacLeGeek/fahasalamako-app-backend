{% extends 'base.html.twig' %}

{% block title %}PrincipesActifs index{% endblock %}

{% block body %}
    <h1>PrincipesActifs index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Expicients</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for principes_actif in principes_actifs %}
            <tr>
                <td>{{ principes_actif.id }}</td>
                <td>{{ principes_actif.nom }}</td>
                <td>
                    <a href="{{ path('principes_actifs_show', {'id': principes_actif.id}) }}">show</a>
                    <a href="{{ path('principes_actifs_edit', {'id': principes_actif.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('principes_actifs_new') }}">Create new</a>
{% endblock %}
